name: KAS Build

on:
  workflow_call:
    inputs:
      kas-config:
        description: 'Colon-separated KAS YAML config string'
        required: true
        type: string
      machine:
        description: >
          Machine name, or a space-separated list for a multiconfig build that
          produces images for several machines in one bitbake invocation. Each
          machine gets its own tarball and artifact; with a single machine the
          artifact names are unchanged.
        required: true
        type: string
      artifact-name:
        description: 'Base name for the build artifact'
        required: true
        type: string
      use-temp-cache:
        description: 'Use runner-local temp directories for SSTATE_DIR/DL_DIR instead of the system persistent cache'
        required: false
        type: boolean
        default: false
      rsync-enabled:
        description: 'rsync matching build outputs to a remote host after the build'
        required: false
        type: boolean
        default: false
      rsync-host:
        description: 'Hostname or IP of the rsync destination'
        required: false
        type: string
        default: ''
      rsync-user:
        description: 'SSH user on the rsync host'
        required: false
        type: string
        default: 'pi'
      rsync-dest:
        description: 'Absolute path on the rsync host to sync into (with trailing slash)'
        required: false
        type: string
        default: ''
      rsync-pattern:
        description: 'Space-separated glob pattern(s) for files to sync from deploy/images/<machine>/. Both the stable-named symlinks and plain files are matched; when a symlink and its target both match, only the symlink (clean name) is synced.'
        required: false
        type: string
        default: '*.swu'
      rsync-machine:
        description: 'Machine whose deploy dir is synced, for multi-machine builds. Defaults to the first entry of machine.'
        required: false
        type: string
        default: ''
      runner:
        description: 'Label of the self-hosted runner to build on. Defaults to the on-prem runner; pass the cloud runner label to build on GCP.'
        required: false
        type: string
        default: 'imx-runner'
    secrets:
      SLACK_WEBHOOK_URL:
        required: true
    outputs:
      version:
        description: 'Resolved git version (git describe) baked into the build'
        value: ${{ jobs.build.outputs.version }}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ${{ inputs.runner || 'imx-runner' }}
    timeout-minutes: 720  # 12 hours — Yocto builds can be long
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true
          # Full history + tags so `git describe` can resolve a meaningful
          # IMDT_DISTRO_VERSION (tag on tag builds, else describe/SHA).
          fetch-depth: 0
          fetch-tags: true
          # Don't persist HTTPS credentials — the self-hosted runner's SSH
          # config handles auth, and HTTPS token setup conflicts with it when
          # this workflow is called from another repo.
          persist-credentials: false

      - name: Determine distro version
        id: version
        run: |
          set -euo pipefail
          VERSION="$(git describe --tags --always --dirty)"
          # Expose as a workflow output so callers can pass it to downstream
          # jobs (e.g. LAVA tests that assert the value baked into the image).
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          # Forward the resolved git version under two names:
          #   IMDT_DISTRO_VERSION  - consumed by repos that set DISTRO_VERSION
          #                          from it (e.g. meta-imdt-renesas/-imx).
          #   META_IMDT_LAYER_VERSION - consumed by repos that surface it as a
          #                          dedicated os-release field (e.g. meta-imdt-qcom-oss).
          # Both carry the same value; consumers opt in via their kas env block.
          echo "IMDT_DISTRO_VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "META_IMDT_LAYER_VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "Resolved version=${VERSION}"

      - name: Ensure LFS objects are materialized
        run: |
          set -e
          if ! command -v git-lfs >/dev/null 2>&1; then
            echo "::error::git-lfs is not installed on this runner — install it (e.g. 'apt-get install git-lfs') so LFS-tracked firmware zips are fetched instead of pointer files."
            exit 1
          fi
          git lfs pull
          missing=0
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            if head -c 40 -- "$f" 2>/dev/null | grep -q '^version https://git-lfs'; then
              echo "::error::$f is still an LFS pointer after 'git lfs pull'"
              missing=1
            fi
          done < <(git lfs ls-files -n)
          [ "$missing" -eq 0 ]

      - name: Configure and create cache directories
        run: |
          if [ "${{ inputs.use-temp-cache }}" = "true" ]; then
            SSTATE_PATH="${RUNNER_TEMP}/sstate-cache"
            DL_PATH="${RUNNER_TEMP}/downloads"
            echo "SSTATE_DIR=${SSTATE_PATH}" >> "$GITHUB_ENV"
            echo "SSTATE_CACHE=${SSTATE_PATH}" >> "$GITHUB_ENV"
            echo "DL_DIR=${DL_PATH}" >> "$GITHUB_ENV"
          else
            SSTATE_PATH="$SSTATE_DIR"
            DL_PATH="$DL_DIR"
          fi
          mkdir -p "$SSTATE_PATH"
          mkdir -p "$DL_PATH"
          # Keep the hashequiv DB alongside the sstate cache. bitbake defaults it
          # to CACHE, i.e. inside the build tree, which oe-core's sanity check
          # warns about once SSTATE_DIR is shared between builds — the hashequiv
          # data is what makes those sstate objects reusable. A caller that
          # exports its own BB_HASHSERVE_DB_DIR (e.g. a central hashserv dir)
          # wins.
          HASHSERVE_DB_PATH="${BB_HASHSERVE_DB_DIR:-$SSTATE_PATH}"
          # bitbake hard-fails if BB_HASHSERVE_DB_DIR is explicitly set to an NFS
          # path (sqlite locking), so leave it at the default there rather than
          # breaking the build to silence a warning.
          if [ "$(stat -f -c %T "$SSTATE_PATH" 2>/dev/null)" = "nfs" ] \
             && [ "$HASHSERVE_DB_PATH" = "$SSTATE_PATH" ]; then
            echo "::notice::SSTATE_DIR is on NFS — leaving BB_HASHSERVE_DB_DIR unset; run a shared hashserv instead"
            echo "KAS_CI_EXTRA_CONFIG=" >> "$GITHUB_ENV"
          else
            # Setting the variable on the host is not enough on its own: no
            # released kas forwards BB_HASHSERVE_DB_DIR into the container —
            # that is siemens/kas 72adc606, still unreleased (4 commits past tag
            # 5.4) — and bitbake's own preserved-env list does not include it
            # either, so it has to arrive via BB_ENV_PASSTHROUGH_ADDITIONS.
            # What every kas does support is an `env:` block, whose keys kas
            # appends to BB_ENV_PASSTHROUGH_ADDITIONS, so declare it there
            # instead and point it at /sstate — the in-container mount of
            # SSTATE_DIR. kas deep-merges config files, so a consumer's own
            # `env:` block (IMDT_DISTRO_VERSION et al) survives.
            #
            # Once the runners run a kas with 72adc606, the host value below is
            # forwarded as /bb-hashserve-db and overrides this default. Both name
            # the same host directory, so either path is correct.
            echo "BB_HASHSERVE_DB_DIR=${HASHSERVE_DB_PATH}" >> "$GITHUB_ENV"
            mkdir -p "$HASHSERVE_DB_PATH"
            # version 14 is accepted by every kas from 4.0 up (4.0 tops out
            # there, 4.7+ accept up to 23), and kas takes the max across merged
            # files, so this cannot pull a consumer's config version down.
            cat > .kas-ci-hashserve.yml <<'EOF'
          header:
            version: 14
          env:
            BB_HASHSERVE_DB_DIR: "/sstate"
          EOF
            echo "KAS_CI_EXTRA_CONFIG=:.kas-ci-hashserve.yml" >> "$GITHUB_ENV"
          fi

      - name: Build ${{ inputs.artifact-name }}
        env:
          KAS_BUILD_DIR: ${{ github.workspace }}/build
          # KAS_CI_EXTRA_CONFIG is appended by the cache-directory step: it adds
          # the generated hashequiv overlay, or is empty when that was skipped.
          KAS_CONFIG: ${{ inputs.kas-config }}
        run: |
          set -euo pipefail
          KAS_CONFIG="${KAS_CONFIG}${KAS_CI_EXTRA_CONFIG:-}"

          echo "=== Building ${{ inputs.artifact-name }} ==="
          echo "=== Commit: $(git rev-parse HEAD) ==="
          echo "=== Branch: ${GITHUB_REF_NAME} ==="
          echo "=== DL_DIR: ${DL_DIR} ==="
          echo "=== SSTATE_DIR: ${SSTATE_DIR} ==="
          echo "=== KAS Config: ${KAS_CONFIG} ==="
          echo "=== Version: ${IMDT_DISTRO_VERSION} ==="

          # kas-container (v4.0) has no -e/--env flag, so forward the version
          # into the container via the runtime args under both supported names.
          # kas then passes whichever the consumer declares through to bitbake
          # (see env: in the consumer's kas includes and BB_ENV_PASSTHROUGH_ADDITIONS).
          kas-container --ssh-dir ~/.ssh/ \
            --runtime-args "-e IMDT_DISTRO_VERSION=${IMDT_DISTRO_VERSION} -e META_IMDT_LAYER_VERSION=${META_IMDT_LAYER_VERSION}" \
            build --update "${KAS_CONFIG}"

      - name: Compress build artifacts
        id: pack
        if: success()
        env:
          MACHINES: ${{ inputs.machine }}
          ARTIFACT_NAME: ${{ inputs.artifact-name }}
          SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          read -ra MACHINE_LIST <<<"$MACHINES"
          # One upload-artifact step per machine, and steps cannot be generated
          # at run time, so the upload steps below are unrolled to a fixed count.
          if [ "${#MACHINE_LIST[@]}" -gt 4 ]; then
            echo "::error::at most 4 machines are supported"
            exit 1
          fi
          # Read back by the release step so it doesn't have to re-derive names.
          MANIFEST="${RUNNER_TEMP}/artifacts.txt"
          : > "$MANIFEST"
          IDX=0
          for MACHINE in "${MACHINE_LIST[@]}"; do
            IDX=$((IDX + 1))
            # A multiconfig build needs a distinct TMPDIR per machine, so the
            # deploy dir is build/tmp<suffix>/deploy/images/<machine>. Newest
            # wins, so a stale dir from an earlier layout is ignored on the
            # persistent self-hosted checkout.
            DIR=$(ls -dt build/tmp*/deploy/images/"$MACHINE" 2>/dev/null | head -n1 || true)
            if [ -z "$DIR" ]; then
              echo "::error::no deploy images directory found for machine '$MACHINE'"
              exit 1
            fi
            # Single-machine builds keep the historical names.
            NAME="$ARTIFACT_NAME"
            if [ "${#MACHINE_LIST[@]}" -gt 1 ]; then
              NAME="${NAME}-${MACHINE}"
            fi
            TAR="${NAME}-${SHA}.tar.zst"
            echo "=== ${MACHINE}: ${DIR} -> ${TAR} ==="
            tar --zstd -cf "$TAR" -C "$DIR" .
            echo "$TAR" >> "$MANIFEST"
            {
              echo "tar-${IDX}=${TAR}"
              echo "name-${IDX}=${NAME}-${SHA}"
            } >> "$GITHUB_OUTPUT"
          done

      - name: Dump resolved KAS config
        if: success()
        env:
          KAS_BUILD_DIR: ${{ github.workspace }}/build
          KAS_CONFIG: ${{ inputs.kas-config }}
        run: |
          # Same config string as the build, overlay included, so the dumped
          # artifact records what was actually built.
          KAS_CONFIG="${KAS_CONFIG}${KAS_CI_EXTRA_CONFIG:-}"
          kas-container --ssh-dir ~/.ssh/ \
            --runtime-args "-e IMDT_DISTRO_VERSION=${IMDT_DISTRO_VERSION} -e META_IMDT_LAYER_VERSION=${META_IMDT_LAYER_VERSION}" \
            dump --resolve-refs --resolve-local --sort "${KAS_CONFIG}" \
            > ${{ inputs.artifact-name }}-${{ github.sha }}-kas-dump.yml
          cp ${{ inputs.artifact-name }}-${{ github.sha }}-kas-dump.yml ${{ inputs.artifact-name }}-kas-dump.yml

      # One artifact per machine, so each board's deploy dir downloads on its own.
      - name: Upload build artifacts
        if: success() && steps.pack.outputs.tar-1 != ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.name-1 }}
          path: ${{ steps.pack.outputs.tar-1 }}
          retention-days: 30

      - name: Upload build artifacts (machine 2)
        if: success() && steps.pack.outputs.tar-2 != ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.name-2 }}
          path: ${{ steps.pack.outputs.tar-2 }}
          retention-days: 30

      - name: Upload build artifacts (machine 3)
        if: success() && steps.pack.outputs.tar-3 != ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.name-3 }}
          path: ${{ steps.pack.outputs.tar-3 }}
          retention-days: 30

      - name: Upload build artifacts (machine 4)
        if: success() && steps.pack.outputs.tar-4 != ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.name-4 }}
          path: ${{ steps.pack.outputs.tar-4 }}
          retention-days: 30

      - name: Upload resolved KAS dump
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name }}-${{ github.sha }}-kas-dump
          path: ${{ inputs.artifact-name }}-${{ github.sha }}-kas-dump.yml
          retention-days: 30

      - name: rsync build outputs to LAVA server
        if: success() && inputs.rsync-enabled
        env:
          MACHINES: ${{ inputs.machine }}
          RSYNC_MACHINE: ${{ inputs.rsync-machine }}
        run: |
          # Sync each matching artifact to the destination with the commit SHA
          # embedded in the filename (before the extension) so every build's
          # artifact is unambiguous, e.g.
          #   imdt-image-base.swu-imdt-8550-sbc.rootfs-abc1234.swu
          #   BOOTAA64-abc1234.EFI
          # rsync-pattern may list several space-separated globs. Both the
          # stable-named symlinks and plain files are matched; when a symlink
          # and the file it points at both match a glob, only the symlink
          # (clean name) is synced so we never upload the timestamped twin.
          SHA="${{ github.sha }}"
          # Only one machine's outputs are synced: on a multi-machine build the
          # boards produce identically named files that would collide at the
          # destination.
          read -ra MACHINE_LIST <<<"$MACHINES"
          MACHINE="${RSYNC_MACHINE:-${MACHINE_LIST[0]}}"
          IMAGES_DIR=$(ls -dt build/tmp*/deploy/images/"$MACHINE" 2>/dev/null | head -n1)
          DEST="${{ inputs.rsync-user }}@${{ inputs.rsync-host }}:${{ inputs.rsync-dest }}"

          upload() {
            SRC="$1"
            BASENAME=$(basename "$SRC")
            STEM="${BASENAME%.*}"
            EXT="${BASENAME##*.}"
            DEST_NAME="${STEM}-${SHA}.${EXT}"
            rsync -av --copy-links "$SRC" "${DEST}${DEST_NAME}"
          }

          # Disable pathname expansion so the glob patterns reach find -name
          # literally instead of being expanded against the build directory.
          set -f
          declare -A SEEN
          for PATTERN in ${{ inputs.rsync-pattern }}; do
            # Symlinks first: they carry the clean (stable) names.
            while IFS= read -r LINK; do
              SEEN["$(readlink -f "$LINK")"]=1
              upload "$LINK"
            done < <(find "$IMAGES_DIR" -maxdepth 1 -type l -name "$PATTERN")
            # Plain files, skipping any already synced via their symlink.
            while IFS= read -r FILE; do
              if [ -z "${SEEN["$(readlink -f "$FILE")"]:-}" ]; then
                upload "$FILE"
              fi
            done < <(find "$IMAGES_DIR" -maxdepth 1 -type f -name "$PATTERN")
          done
          set +f

      - name: Generate release notes from commits since the last tag
        if: success() && startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          # The release body is built up in this file: the changelog first, then
          # any asset-handling instructions appended by the step below.
          # RUNNER_TEMP rather than the workspace so the notes never land in a
          # build directory or survive into an unrelated job on the persistent
          # self-hosted checkout.
          NOTES="${RUNNER_TEMP}/release-notes.md"
          TAG="${GITHUB_REF_NAME}"

          # Nearest release tag reachable from this one, looked up within the
          # same tag series so repos that carry more than one (this workflow is
          # shared, and tag schemes differ: 'v1.2.3', 'imdt-pico-bsp-v1.5.0',
          # 'imdt-v2h-bsp-v1.0.0') compare against their own predecessor rather
          # than whichever tag happens to be nearest. The series is the tag up
          # to its first digit, so 'imdt-pico-bsp-v1.5.0' matches
          # 'imdt-pico-bsp-v[0-9]*'. Tag names cannot contain glob characters
          # (git check-ref-format rejects them), so this is safe to splice into
          # a pattern.
          SERIES="${TAG%%[0-9]*}"
          if [ "$SERIES" = "$TAG" ]; then
            # No digits at all — a one-off name rather than a version. Fall back
            # to the nearest ancestor tag of any kind.
            PREV="$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || true)"
          else
            PREV="$(git describe --tags --match "${SERIES}[0-9]*" --abbrev=0 "${TAG}^" 2>/dev/null || true)"
          fi
          # Empty PREV means the first release in this series; every commit up to
          # the tag is then listed. Both lookups rely on the full history and
          # tags fetched by the checkout step above (fetch-depth: 0,
          # fetch-tags: true).
          if [ -n "$PREV" ]; then
            RANGE="${PREV}..${TAG}"
          else
            RANGE="$TAG"
          fi

          # Which section an area prefix belongs to. Commit subjects across the
          # IMDT layer repos are written as "<area>: Summary", sometimes with
          # several slash-separated areas ("meta/kas:", "ci/lava:"); the
          # leftmost known area wins, so the prefix reads
          # most-significant-area-first. Anything unrecognised (including
          # subjects with no prefix at all) collects under "Other changes"
          # rather than being dropped.
          declare -A CAT_OF=(
            [feat]=features [feature]=features
            [fix]=fixes [bugfix]=fixes
            [kernel]=kernel [linux]=kernel [dt]=kernel [dts]=kernel
            [firmware]=kernel [u-boot]=kernel [u-boot-concept]=kernel
            [bootloader]=kernel [atf]=kernel [tf-a]=kernel [optee]=kernel
            [graphics]=kernel [gpu]=kernel [camera]=kernel [audio]=kernel
            [display]=kernel [npu]=kernel
            [meta]=bsp [recipe]=bsp [recipes]=bsp [kas]=bsp [distro]=bsp
            [machine]=bsp [layer]=bsp [conf]=bsp [image]=bsp [swupdate]=bsp
            [ci]=ci [lava]=ci [test]=ci [tests]=ci [gha]=ci [workflow]=ci
            [docs]=docs [doc]=docs [readme]=docs
          )
          CAT_ORDER=(features fixes kernel bsp ci docs other)
          declare -A CAT_TITLE=(
            [features]="Features"
            [fixes]="Fixes"
            [kernel]="Kernel, firmware & drivers"
            [bsp]="BSP, layers & recipes"
            [ci]="CI & testing"
            [docs]="Documentation"
            [other]="Other changes"
          )

          declare -A LINES=()
          COUNT=0
          while IFS=$'\t' read -r SHA SUBJECT; do
            [ -n "$SHA" ] || continue
            COUNT=$((COUNT + 1))
            CAT=other
            PREFIX="${SUBJECT%%: *}"
            # A prefix only counts when the subject really had a ": " separator
            # and the part before it looks like an area tag rather than prose.
            if [ "$PREFIX" != "$SUBJECT" ] && [[ "$PREFIX" =~ ^[A-Za-z0-9._/+-]{1,32}$ ]]; then
              IFS='/' read -ra AREAS <<<"${PREFIX,,}"
              for AREA in "${AREAS[@]}"; do
                if [ -n "${CAT_OF[$AREA]:-}" ]; then
                  CAT="${CAT_OF[$AREA]}"
                  break
                fi
              done
            fi
            LINES[$CAT]+="- ${SUBJECT} (\`${SHA}\`)"$'\n'
          done < <(git log --no-merges --format='%h%x09%s' "$RANGE" --)

          if [ -n "$PREV" ]; then
            SINCE="since \`${PREV}\`"
          else
            SINCE="in the first tagged release"
          fi

          {
            echo "## What's changed"
            echo ""
            if [ "$COUNT" -eq 0 ]; then
              # Possible when a tag is moved or re-cut on a commit that is
              # already released.
              echo "No commits ${SINCE}."
            else
              if [ "$COUNT" -eq 1 ]; then
                echo "1 commit ${SINCE}."
              else
                echo "${COUNT} commits ${SINCE}."
              fi
              for CAT in "${CAT_ORDER[@]}"; do
                [ -n "${LINES[$CAT]:-}" ] || continue
                echo ""
                echo "### ${CAT_TITLE[$CAT]}"
                echo ""
                printf '%s' "${LINES[$CAT]}"
              done
            fi
            echo ""
            echo "**Build:** \`${{ steps.version.outputs.version }}\` · [CI run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            if [ -n "$PREV" ]; then
              echo ""
              echo "**Full changelog:** ${{ github.server_url }}/${{ github.repository }}/compare/${PREV}...${TAG}"
            fi
          } > "$NOTES"

          echo "=== Generated release notes (${RANGE}) ==="
          cat "$NOTES"

      - name: Prepare release assets (split if over GitHub's 2 GiB limit)
        if: success() && startsWith(github.ref, 'refs/tags/')
        env:
          SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          NOTES="${RUNNER_TEMP}/release-notes.md"
          # Assets are staged in their own directory so the release glob picks
          # them up without also matching the SHA-named tarballs they came from.
          OUT="${RUNNER_TEMP}/release"
          rm -rf "$OUT"
          mkdir -p "$OUT"
          # GitHub release assets are capped at 2 GiB per file. Split below that
          # with headroom so the upload never bumps the ceiling.
          CHUNK_MIB=1900
          LIMIT=$(( CHUNK_MIB * 1024 * 1024 ))
          SPLIT=0
          while IFS= read -r SRC; do
            [ -n "$SRC" ] || continue
            # The build artifact carries the commit SHA so per-build uploads never
            # collide; the *release* assets use a clean, SHA-free name so consumers
            # can wget them from a stable .../releases/latest/download/<name> URL.
            RELEASE="$(basename "${SRC/-${SHA}/}")"
            SIZE=$(stat -c%s "$SRC")
            if [ "$SIZE" -ge "$LIMIT" ]; then
              echo "${RELEASE} is ${SIZE} bytes (>= ${LIMIT}); splitting into ${CHUNK_MIB} MiB numbered parts."
              # Produces RELEASE.part00, .part01, … that reassemble byte-for-byte
              # with `cat RELEASE.part* > RELEASE`.
              split --bytes="${CHUNK_MIB}M" --numeric-suffixes --suffix-length=2 "$SRC" "${OUT}/${RELEASE}.part"
              # Checksum recorded against the reassembled (clean) name so a consumer
              # can `sha256sum -c` it straight after concatenating the parts.
              echo "$(sha256sum "$SRC" | cut -d' ' -f1)  ${RELEASE}" > "${OUT}/${RELEASE}.sha256"
              SPLIT=1
            else
              echo "${RELEASE} is ${SIZE} bytes (< ${LIMIT}); uploading as a single asset."
              cp "$SRC" "${OUT}/${RELEASE}"
            fi
          done < "${RUNNER_TEMP}/artifacts.txt"
          ls -lh "$OUT"
          if [ "$SPLIT" = "1" ]; then
            # Appended to the notes written by the previous step, so the release
            # body reads: changelog, then how to put the asset back together.
            {
              echo ""
              echo "### Reassembling split assets"
              echo ""
              echo "Images over GitHub's 2 GiB per-file release limit are split into"
              echo "numbered parts. Download every \`<name>.part*\` file, then:"
              echo ""
              echo '```sh'
              echo "cat <name>.part* > <name>"
              echo "sha256sum -c <name>.sha256"
              echo '```'
            } >> "$NOTES"
          fi

      - name: Create GitHub release
        if: success() && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          # The staging dir holds one clean-named tarball per machine, or its
          # .part*/.sha256 set when that tarball had to be split.
          #
          # The body is replaced (not appended to) so re-running a tag build
          # regenerates the notes in place instead of stacking another copy
          # underneath the previous one.
          body_path: ${{ runner.temp }}/release-notes.md
          files: |
            ${{ runner.temp }}/release/*
            ${{ inputs.artifact-name }}-${{ github.sha }}-kas-dump.yml
            ${{ inputs.artifact-name }}-kas-dump.yml

  notify:
    runs-on: ubuntu-latest
    needs: build
    if: always()
    steps:
      - name: Build notification message
        id: msg
        run: |
          RESULT="${{ needs.build.result }}"
          if [ "$RESULT" = "success" ]; then EMOJI="✅"; else EMOJI="❌"; fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          MSG="${EMOJI} *${{ github.workflow }}* — ${RESULT}"
          MSG="${MSG}\n"
          MSG="${MSG}\n*Branch:* ${GITHUB_REF_NAME}"
          MSG="${MSG}\n*Commit:* ${{ github.sha }}"
          MSG="${MSG}\n*Triggered by:* ${{ github.actor }}"
          MSG="${MSG}\n"
          MSG="${MSG}\n<${RUN_URL}|View run>"

          echo "text=${MSG}" >> $GITHUB_OUTPUT

      - name: Send Slack notification
        uses: slackapi/slack-github-action@v2
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: webhook-trigger
          payload: |
            {
              "text": "${{ steps.msg.outputs.text }}"
            }

  announce-release:
    runs-on: ubuntu-latest
    needs: build
    # Only when a tag was pushed and its build succeeded — a tag build that
    # passes is a new release worth announcing.
    if: needs.build.result == 'success' && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Build release announcement
        id: msg
        run: |
          TAG="${GITHUB_REF_NAME}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          RELEASE_URL="${{ github.server_url }}/${{ github.repository }}/releases/tag/${TAG}"

          MSG="🚀 *New release: ${TAG}*"
          MSG="${MSG}\n"
          MSG="${MSG}\n*Repository:* ${{ github.repository }}"
          MSG="${MSG}\n*Commit:* ${{ github.sha }}"
          MSG="${MSG}\n*Released by:* ${{ github.actor }}"
          MSG="${MSG}\n"
          MSG="${MSG}\n<${RELEASE_URL}|View release> · <${RUN_URL}|View build>"

          echo "text=${MSG}" >> $GITHUB_OUTPUT

      - name: Send Slack release announcement
        uses: slackapi/slack-github-action@v2
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: webhook-trigger
          payload: |
            {
              "text": "${{ steps.msg.outputs.text }}"
            }
